@page
@model Sneaker_Store.Pages.common.KurvIndexModel

<!DOCTYPE html>
<html>
<head>
    <title>Basket</title>
</head>
<body>
<h1>Your Basket</h1>

@if (Model.Skos != null && Model.Skos.Any())
{
    <table>
        <thead>
        <tr>
            <th>Image</th>
            <th>Shoe Name</th>
            <th>Brand</th>
            <th>Size</th>
            <th>Price</th>
            <th>Action</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var sko in Model.Skos)
        {
            <tr>
                <td><img src="@sko.ImageUrl" alt="@sko.Model" width="100" /></td>
                <td>@sko.Model</td>
                <td>@sko.Maerke</td>
                <td>@(sko.Str.HasValue ? sko.Str.ToString() : "N/A")</td> <!-- Display size if available, otherwise "N/A" -->
                <td>@sko.Pris.ToString("C")</td>
                <td>
                    <form method="post" asp-page-handler="DeleteFromBasket">
                        <input type="hidden" name="SkoId" value="@sko.SkoId" />
                        <button type="submit" class="btn-danger">X</button>
                    </form>
                </td>
            </tr>
        }
        </tbody>
    </table>
    <p>Total Price: @Model.Skos.Sum(s => s.Pris).ToString("C")</p>
}
else
{
    <p>Your basket is empty.</p>
}
<form method="post" class="col-6">
    <button type="submit" formaction="/common/OrderConfirm" class="alert-success btn-lg btn-block">Køb</button>

<a asp-page="/common/SkoIndex" class="btn btn-primary btn-lg btn-block col-6" >Back to Shop</a>
</form>
</body>

</html>